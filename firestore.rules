rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Offene Regeln für authentifizierte Benutzer
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
